# Production Environment Configuration Template

# Database Configuration
MONGODB_URI=mongodb+srv://your-username:your-password@your-cluster.mongodb.net/shiteni?retryWrites=true&w=majority

# NextAuth Configuration (IMPORTANT: Update for production)
NEXTAUTH_URL=https://your-domain.com
NEXTAUTH_SECRET=your-super-secure-secret-key-here

# JWT Configuration
JWT_SECRET=your-jwt-secret-key-here

# Email Configuration
SMTP_HOST=mail.shiteni.com
SMTP_PORT=587
SMTP_USERNAME=Support@shiteni.com
SMTP_PASSWORD=@M309877321k.
SMTP_FROM_EMAIL=Support@shiteni.com
SMTP_FROM_NAME=Shiteni Support
SMTP_ENCRYPTION=tls

# Email Verification Settings
EMAIL_VERIFICATION_ENABLED=true

# Application Configuration
NODE_ENV=production

# Lipila Payment Gateway Configuration
LIPILA_SECRET_KEY=your-actual-lipila-api-key-here
LIPILA_BASE_URL=https://lipila-prod.hobbiton.app
LIPILA_CURRENCY=ZMW
LIPILA_MOCK_MODE=false

# CORS Configuration (if needed)
ALLOWED_ORIGINS=https://your-domain.com

# Instructions for Production Deployment:
# 1. Copy this file to your deployment platform's environment variables
# 2. Replace all placeholder values with your actual production values
# 3. Generate secure secrets using: openssl rand -base64 32
# 4. Update NEXTAUTH_URL to your production domain
# 5. Ensure MongoDB Atlas is configured for production
# 6. Test email functionality with your SMTP settings
# 7. Configure Lipila with your real API credentials
# 8. Set LIPILA_MOCK_MODE=false for real payments
